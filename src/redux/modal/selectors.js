export const selectIsModalOpen = state => state.modal.isModalOpen;
export const selectModalActionType = state => state.modal.actionType;
export const selectModalData = state => state.modal.modalData;
